import Results from "../components/Results/Results";
import Header from "./Header";

const MainContainer = () => {
    return (
        <main>
            <Header />
            <Results />
        </main>
    );
}

export default MainContainer;